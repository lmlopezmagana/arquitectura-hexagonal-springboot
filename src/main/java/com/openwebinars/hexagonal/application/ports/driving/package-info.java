package com.openwebinars.hexagonal.application.ports.driving;

/**
 * Aquí podrían estar definidas las interfaces de los casos de uso, que luego podrían implementarse.
 *
 * Algunos autores crean un puerto por "encima" de los casos de uso, algo así como un InBoundaryPort
 * que tiene métodos que reciben objetos request, y devuelven DTOs, de forma que los controladores
 * o bien implementan estas interfaces, o bien utilizan el adaptador de la misma para que en los
 * métodos de controlador solamente haya una línea de código.
 *
 */